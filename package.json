{
  "name": "micra-platform",
  "private": true,
  "version": "0.0.0-bootstrap",
  "scripts": {
    "lint": "echo 'lint: implementation pending'",
    "typecheck": "echo 'typecheck: implementation pending'",
    "test": "echo 'test: implementation pending'",
    "build": "echo 'build: implementation pending'"
  }
}
